/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    trailingSlash: false,
    images: {
        domains: [
            "hudozkacdn.tmshv.com",
            "images.weserv.nl",
        ],
    },
    /* config options here */
}

export default nextConfig
