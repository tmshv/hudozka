extends ../layout

block custom_head
	link(rel="stylesheet", href="/s/document.css")

block content
	.menu.opac
		h1
			span.bold ДХШ 
			span.norm Шлиссельбурга, 
			span.norm #{doc.title}
	.doclist
		for a in doc.url
			img(src='#{a}')